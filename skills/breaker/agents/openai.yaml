interface:
  display_name: "✂️ Breaker"
  short_description: "Open a PR with size-limit checks; splits oversized diffs into independent SRP-based branches"
  default_prompt: "$breaker 1000"
policy:
  allow_implicit_invocation: false
